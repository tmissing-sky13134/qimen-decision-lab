import type { Palace } from "@/lib/types";
const order = [4, 9, 2, 3, 5, 7, 8, 1, 6];
export function QimenGrid({ palaces }: { palaces: Palace[] }) { const byNumber = new Map(palaces.map((p) => [p.number, p])); return <div className="grid grid-cols-3 overflow-hidden rounded-xl border border-[#bfcabe] bg-[#bfcabe] gap-px">{order.map((number) => { const p = byNumber.get(number)!; return <article className="palace p-3" key={number}><div className="flex items-start justify-between"><div><span className="font-serif text-2xl">{p.name}{p.number}宫</span></div>{p.void && <span className="rounded bg-clay/10 px-1.5 py-0.5 text-[10px] font-bold text-clay">空亡</span>}</div><p className="mt-1 text-[10px] tracking-wider text-slate-400">{p.direction}</p><div className="mt-3 grid grid-cols-2 gap-1 text-[10px] text-slate-400"><span>地盘干</span><span>天盘干</span><span className="font-serif text-xl text-clay">{p.earthStem}</span><span className="font-serif text-xl text-moss">{p.skyStem}</span></div><div className="mt-3 grid grid-cols-3 gap-x-1 text-xs text-slate-600"><span><i className="block text-[10px] not-italic text-slate-400">九星</i>{p.star ?? "—"}</span><span><i className="block text-[10px] not-italic text-slate-400">八门</i>{p.door ?? "—"}</span><span><i className="block text-[10px] not-italic text-slate-400">八神</i>{p.deity ?? "—"}</span></div>{p.markers?.length ? <div className="mt-2 flex flex-wrap gap-1">{p.markers.map((m) => <span key={m} className="rounded bg-mist px-1.5 py-0.5 text-[10px] text-moss">{m}</span>)}</div> : null}</article>; })}</div>; }
